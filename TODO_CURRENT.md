# UI/UX Fixes - TODO

1. [x] Fix `static/js/app.js` — Add `copyTextToClipboard()` helper, update copy functions to use toasts
2. [x] Fix `templates/index.html` — Grey Open button, icon-only copy clone button, full command visible
3. [x] Fix `templates/repo.html` — Full git clone command row with copy icon
4. [x] Fix `static/css/style.css` — Clone command scrollable, new icon button styles, alignment
5. [ ] Test app starts without errors

